import { exampleText } from "./example-text"

  test('Inverts antonyms in string', () => {
    const antonyms = [
      ["above", "below"],
      ["far", "near"],
      ["from", "to"],
      ["in", "out"],
      ["is", "isn't"],
      ["over", "under"],
      ["will", "won't"],
    ]

    expect(exampleText).toContain("")
  })
